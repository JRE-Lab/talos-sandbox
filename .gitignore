# Secrets — NEVER commit the real .env (holds OPENAI_API_KEY).
.env

# Python bytecode / caches
__pycache__/
*.pyc
.pytest_cache/

# Virtual environments
.venv/
venv/

# Caddy runtime state (certs, config) — managed by the container volumes
caddy_data/
caddy_config/

# Logs
*.log
