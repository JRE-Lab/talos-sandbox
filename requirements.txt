# TALOS Sandbox — Python dependencies
# Pinned to reasonable version floors; the replay path needs none of the
# network/LLM bits at runtime, but they ship so live mode works when enabled.

fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
sse-starlette>=2.0
python-dotenv>=1.0
openai>=1.40

# --- test/dev only ---
pytest>=8.0
httpx>=0.27
